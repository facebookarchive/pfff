(* 
 * The AST in lang_java/parsing/ is actually already a simple AST
 * as opposed to what we usually have in other language
 * directories (e.g. lang_php/). So we just have a simple
 * type alias here.
 *)
type program = Ast_java.program
